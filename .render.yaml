services:
  - type: web
    name: discord-api
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn rat.app:application"
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHONPATH
        value: /opt/render/project/src
